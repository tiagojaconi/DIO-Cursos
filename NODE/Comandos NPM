npm init (instalar pacotes)
server (nome do pacote para baixar)
npm init -y (baixa o pacote sem pedir confirmações)

node (abre o node no terminal)
http (mostra varias funções e informações de http, como os códigos de erro)
.exit (sai do node)

Ctrl + c (finaliza a execução no terminal)

